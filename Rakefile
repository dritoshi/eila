desc "Make demo of project template"
task :default => [:test]

desc "eila create demo"
task :demo do
  ruby "-Ilib ./bin/eila create demo"
end    

desc "Unit Test for eila.rb"
task :test do
  ruby "-Ilib test/unittest.rb"
end
